---
milestone: v1.0
status: gaps_found
audited: 2026-05-28
mode: autonomous
auditor: orchestrator (no integration-checker subagent available in environment)
---

# Milestone v1.0 Audit

## Scope

- **Milestone:** v1.0 — `oplya` marketplace + `zapili` plugin v1
- **Phases:** 6 (all complete: 1, 2, 3, 4, 5, 6)
- **Plans:** 20 (all complete)
- **Requirements:** 43/43 mapped (43 marked Complete in REQUIREMENTS.md traceability)

## Phase-level verification rollup

| Phase | Status | Source |
|-------|--------|--------|
| 1 | passed | `.planning/phases/01-marketplace-plugin-skeleton/01-VERIFICATION.md` |
| 2 | passed | `02-VERIFICATION.md` |
| 3 | passed | `03-VERIFICATION.md` |
| 4 | passed | `04-VERIFICATION.md` |
| 5 | passed | `05-VERIFICATION.md` |
| 6 | passed | `06-VERIFICATION.md` |

All six phases produced VERIFICATION.md files. None reported critical blockers; each closed with `status: passed`.

## Requirements coverage (3-source cross-reference)

| Source | Method | Count |
|--------|--------|-------|
| REQUIREMENTS.md traceability table | `grep '\| Complete \|'` | 43 |
| Phase VERIFICATION.md "Requirements coverage" sections | sum of `Complete` entries | 43 |
| Per-plan SUMMARY.md frontmatter `requirements_satisfied` | distinct REQ-IDs union | 43 |

All three sources agree: 43 of 43 v1 requirements covered.

## Cross-phase integration (manual audit — no integration-checker available)

| Integration boundary | Status | Notes |
|----------------------|--------|-------|
| Phase 2 `commands/zapili.md` → Phase 4 `Skill(orchestrator)` | OK | `commands/zapili.md` Step 2 invokes the skill; `allowed-tools` declares `Skill(orchestrator)`. |
| Phase 3 schemas → Phase 4 wrappers | OK | `codex-validate-research.sh` + `codex-validate-plan.sh` reference `validation-findings.schema.json` by path. |
| Phase 3 reference docs → Phase 4 SKILL.md | OK | SKILL.md "Loading order" section requires reading `contracts.md`, `task-sizing.md`, `codex-prompts.md` before doing anything else. |
| Phase 4 SKILL.md Stage 5 (planner) → Phase 4 `agents/planner.md` | OK | Stage 5 dispatches `Agent(subagent_type="planner", ...)`; `planner.md` `name: planner` matches. |
| Phase 4 SKILL.md Stage 7 (engineer) → Phase 5 `agents/engineer.md` | OK | Stage 7a dispatches `Agent(subagent_type="engineer", ...)`; `engineer.md` `name: engineer` matches; `allowed-tools` lists `Agent(researcher, planner, engineer)`. |
| Phase 4 `state.sh` → Phase 6 `derive-stage.sh` | OK | Both target the same `.zapili/state.json` schema (state.schema.json); derive-stage.sh is the artifact-first authority per ZAP-53. |
| Phase 5 SKILL.md Stage 7 (single-phase) → Phase 6 Stage 7 (wave parallel) | OK | Phase 6 replaced the entire Stage 7 body; PHASE-6-STUB block was removed. |
| Phase 6 `summarize.sh` → SKILL.md Stage 8 | OK | Stage 8 invokes `summarize.sh`; smoke-tested on synthetic PHASE artifacts and SUMMARY.md was correctly generated with the completion sentinel. |
| `plugin.json` v1.0.0 version bump | OK | `validate-manifests.sh` re-run after the bump still passes. |

## Anti-patterns scan

- TODO/FIXME in shipped code: none (`grep -RIn 'TODO\|FIXME' plugins/zapili/scripts plugins/zapili/skills plugins/zapili/agents plugins/zapili/commands plugins/zapili/hooks plugins/zapili/.claude-plugin` → no matches).
- Empty stubs in `commands/`/`agents/`: none — every agent/command has a substantive body.
- Forbidden vocabulary (`key`, `main`, `top`, `important`) appearing outside backticked enumerations: NONE (verified across SKILL.md, all agent files, all script-embedded prompts).
- PHASE-N-STUB placeholders remaining in shipped artifacts: NONE (PHASE-5-STUB and PHASE-6-STUB both substituted in Phase 5 and Phase 6 respectively).
- `.gitkeep` placeholders: NONE.

## Deferred / open items (acknowledged tech debt — NOT release blockers)

| Item | Status | Routed to |
|------|--------|-----------|
| Live chaos rehearsal (11 boundaries) | DEFERRED | `chaos-rehearsal-LOG.md` stamps procedure; release maintainer executes manually. |
| Live smoke-test round-trip on `smoke-small-task` | DEFERRED | `plugins/zapili/tests/fixtures/smoke-small-task/README.md` documents procedure. |
| Live codex calibration against the 5 reviewer fixtures | DEFERRED | Phase 3 D-10 + Phase 6 stance: calibration is dev-time / pre-release. |
| Pre-commit gate that runs `validate-schemas.sh` automatically | DEFERRED | v2 polish (TOOL-02-ish). |
| Live `/plugin install zapili@oplya` rehearsal after Phase 6's v1.0.0 bump | DEFERRED | Phase 1's install rehearsal pre-dated the version bump; release maintainer should re-rehearse. |

These items are documented as gaps because the project's Definition of Done (REQUIREMENTS.md) explicitly mentions "manual smoke test against the acceptance criteria passes" — that manual step is the release maintainer's responsibility and has not been executed inside the autonomous Phase 6 run (no TTY harness available).

## Gaps

### CRITICAL — none

### NON-CRITICAL (release-time manual steps required)

1. **Live install rehearsal post-version-bump** — Phase 1's rehearsal stamp was pre-v1.0.0. Maintainer must re-run `/plugin marketplace add nepavel/oplya` + `/plugin install zapili@oplya` after pushing the v1.0.0 commit to confirm the install path still resolves.
2. **Live smoke-test round-trip** — execute `plugins/zapili/tests/fixtures/smoke-small-task/README.md` procedure end-to-end.
3. **Live chaos rehearsal** — execute the 11-boundary procedure in `plugins/zapili/tests/chaos/README.md`; append per-boundary PASS/FAIL to `chaos-rehearsal-LOG.md`.
4. **Live codex review calibration** — run codex against each fixture in `plugins/zapili/tests/fixtures/f1..f5/` and verify every `expected-findings.json` ID appears in the codex output.

These are gated on a live Claude Code session + codex auth — they cannot be performed inside the autonomous orchestrator. They are documented in their respective READMEs/LOGs so the release maintainer can execute them.

## Status: gaps_found

The four non-critical gaps above are RELEASE-TIME maintainer activities. The artifact-level milestone is complete (all 43 requirements implemented, all phase VERIFICATIONs passed, all integration boundaries verified by inspection). The maintainer must run the manual rehearsals before tagging v1.0.0 in git.

## Recommendation

Proceed to `complete-milestone` with `gaps_found` accepted — the gaps are documented manual rehearsal steps that the release process owns, not implementation gaps.
