# Milestone v1.1 Requirements — SHIPPED

**Shipped:** 2026-05-29
**Phases:** 7–8
**Total Requirements:** 6/6 ✅

Hardening + capability extension on top of v1.0.0 (the 43 v1 requirements
shipped in milestone v1.0 are not duplicated here — see
`.planning/milestones/v1.0-REQUIREMENTS.md` when that archive lands).

## Review follow-ups cleanup (ZAP-followups) — Phase 7

All five close non-blocking follow-ups surfaced by the v1.0.0 ultra-principal code review. None blocked end-to-end execution; together they raised v1.0 hardening to release-grade and fixture-coverage to 5/5 for codex calibration rehearsals.

- [x] **ZAP-55** ✅ — `plugins/zapili/agents/planner.md` declares an optional `<file role="prior-findings">` input in its `<inputs>` block; the `<task>` section explicitly instructs the planner how to address each prior HIGH/MEDIUM finding by ID on a fix iteration, cite each finding ID in the revision's `flagged_gaps` for traceability, and avoid removing phases to hide gaps (mirrors `engineer.md`'s existing prior-findings handling). Closes review finding C-03. **Outcome:** validated by inspection + Phase 7 SUMMARY 07-01.
- [x] **ZAP-56** ✅ — Orchestrator Stage 5 (post-planner) parses `flagged_gaps` from the planner payload; if non-empty, surfaces each gap to the user via `AskUserQuestion`, appends the user's answers to `CONTEXT.md` under a new `## Gap Resolutions` section, and only then advances to `plan_validate`. Empty `flagged_gaps` array continues silently. Closes review finding C-04. **Outcome:** validated by inspection of SKILL.md Stage 5.5 + Phase 7 SUMMARY 07-01.
- [x] **ZAP-57** ✅ — `plugins/zapili/tests/fixtures/README.md` calibration loop documents the correct per-role wrapper invocation (`codex-validate-research.sh <task> <context>`, `codex-validate-plan.sh <plan> <phase_glob>`, `codex-review-phase.sh <task> <phase> <engineer_payload>`); fixture `f3-plan-ambiguity/` includes a minimal `PLAN.md`; fixtures `f4-phase-missing-tests/` and `f5-phase-style-drift/` each include a minimal `TASK.md`. Every fixture in `tests/fixtures/f1..f5` and `smoke-small-task` runnable end-to-end via the documented loop. Closes review findings F-01 and F-02. **Outcome:** validated by live calibration evidence in commit `fb5f4ce` (all 4 fixtures successfully prog'd against codex 0.133.0).
- [x] **ZAP-58** ✅ — `plugins/zapili/scripts/check-codex.sh` uses `set -euo pipefail` (was `set -uo pipefail`); existing `if ! ... ; fi` and `|| true` guards continue to work under `-e`; SessionStart hook still exits 0 on missing/unauthenticated codex (advisory contract per ZAP-02 preserved). CLAUDE.md hook discipline and Phase 2 D-15 honored. Closes review finding H-01. **Outcome:** validated by `bash check-codex.sh </dev/null` exits 0; Phase 7 SUMMARY 07-03 documents.
- [x] **ZAP-59** ✅ — `plugins/zapili/scripts/check-wave-disjointness.sh` regex pattern accepts both production naming (`PHASE-01`, `PHASE-02`) and fixture naming (`PHASE-XX-a`, `PHASE-XX-b`); the f2 fixture's PHASE files are detected, intra-wave write-set intersection is computed, and the seeded overlap surfaces as `kind: "write-scope-overlap"`. Closes review finding S-01. **Outcome:** validated by running script against f2 (exit 1 with OVERLAP diagnostic) and f3 (exit 0 — no overlap).

## Codex self-fix fallback (ZAP-self-fix) — Phase 8

New capability — the escape hatch for when the engineer/planner subagent has reached its own ceiling on an issue and codex (independent context, different model family) can resolve it.

- [x] **ZAP-60** ✅ — After the codex review fix-loop (Stage 4 `plan_validate` or Stage 6 `phase_review`) hits its iteration cap (default 4, configurable via `.zapili/state.json` `.fix_loop_cap`) with at least one persistent HIGH finding, the orchestrator dispatches `plugins/zapili/scripts/codex-self-fix.sh <artifact> <validator_role> <prior_findings_json>` instead of halting. The fixer composes a "fixer-role" prompt (documented as a fourth role in `references/codex-prompts.md`) instructing codex to emit a unified-diff patch wrapped in `<response><patch>...</patch></response>`; the orchestrator dry-runs the patch via `git apply --check`, persists it under `.zapili/codex-self-fix-attempt-N.patch`, then applies it via `git apply`; the original validator re-runs against the patched artifact. Termination: (a) post-fix re-review clean → continue workflow; (b) codex emits an empty patch → halt with `## CODEX SELF-FIX EXHAUSTED — no diff produced`; (c) post-fix re-review still has HIGH → halt with the unresolved finding IDs + patch path. **Outcome:** validated by live 4-iteration round-trip against codex 0.133.0 (Phase 8 LOG + f6 post-fix-review JSON in commit `fb5f4ce`); seeded HIGH `ISS-23ba7d51473d` resolved by the codex-generated patch.

## Post-implementation review rounds (no new requirements; quality work)

Three rounds of ultra-principal code review caught issues the in-thread audit missed:

| Round | Commit | Findings closed |
|-------|--------|-----------------|
| Round 1 (post-Phase-8) | `c870186` | 3 BLOCKERs (codex-self-fix double-invocation, f6 false-positive overlap, schema missing fix_loop_cap) + 3 WARNINGs |
| Round 2 (exhaustiveness contract) | `78a5f30` | Defeats codex's default targeted-review mode; schema gains 4 optional fields |
| Round 3 (release-prep) | `b31d95a` | 3 P1 (Russian text in prod prompts, phase-changes schema max=3, README v1.0.0) + 5 P2 |

These rounds are documented in `.planning/v1.1-MILESTONE-AUDIT.md` under "Post-audit ultra-principal review rounds".

## Adjusted / dropped requirements

None — all 6 requirements shipped as originally specified.

## Outcomes summary

- **6/6 v1.1 requirements complete** (49/49 cumulative across v1 + v1.1).
- **Schema back-compat preserved:** all 4 schema extensions are optional; existing `expected-findings.json` fixtures unchanged.
- **Live calibration coverage:** 5/5 codex validator fixtures prog'd end-to-end against codex-cli 0.133.0 (f1 inline-verified during Round 2; f2..f5 persisted in commit `fb5f4ce`; f6 post-fix re-review proves codex self-fix end-to-end).
- **Plugin version:** bumped `1.0.0` → `1.1.0` in `plugins/zapili/.claude-plugin/plugin.json` at release commit `1a503ab`.
- **English-only audit passed** post-Round-3 — no Russian text in any shipped artifact (only historical Phase 8 calibration prompt under `f6/.zapili/` retains the original phrase; not shipped via plugin install).

---

*Milestone archived: 2026-05-29*
