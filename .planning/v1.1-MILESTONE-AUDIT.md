---
milestone: v1.1
status: passed
audited: 2026-05-28
last_revised: 2026-05-29
mode: autonomous + post-audit ultra-principal review (2 rounds) + exhaustiveness contract
auditor: orchestrator (live codex 0.133.0 calibration available; no Agent subagent dispatch in environment — author/audit done in main thread)
---

# Milestone v1.1 Audit

## Scope

- **Milestone:** v1.1 — hardening + codex self-fix
- **Phases:** 2 new phases (7 + 8); cumulative 8 of 8
- **Plans:** 6 new plans (3 in Phase 7, 3 in Phase 8); cumulative 26 of 26
- **Requirements:** 6/6 v1.1 requirements complete (ZAP-55..60); cumulative 49/49

## Phase-level verification rollup

| Phase | Status | Source |
|-------|--------|--------|
| 7 | passed | `.planning/phases/07-review-followups-cleanup/07-VERIFICATION.md` |
| 8 | passed | `.planning/phases/08-codex-self-fix-fallback/08-VERIFICATION.md` |

Both phases produced VERIFICATION.md. Phase 8 additionally produced `live-codex-calibration-LOG.md` documenting a successful live round-trip against codex-cli 0.133.0.

## Requirements coverage (3-source cross-reference)

| Source | Method | Count |
|--------|--------|-------|
| REQUIREMENTS.md ZAP-55..60 entries | `grep '\[x\] \*\*ZAP-' .planning/REQUIREMENTS.md \| wc -l` constrained to 55..60 | 6 |
| Phase VERIFICATION.md "Requirements closed" sections | sum across 07 + 08 | 6 |
| Per-plan SUMMARY.md "Requirements closed/progressed" | distinct ZAP-IDs union | 6 |

All three sources agree: ZAP-55, ZAP-56, ZAP-57, ZAP-58, ZAP-59, ZAP-60 — 6/6 v1.1 requirements complete.

## Cross-phase integration

| Integration boundary | Status | Notes |
|----------------------|--------|-------|
| Phase 7 planner.md `prior-findings` contract → orchestrator Stage 5.5 routing | OK | Phase 7 Plan 01 implemented both sides in a single commit; the planner now consumes `prior-findings` and emits `flagged_gaps` with `fix:ISS-...` topic prefix; SKILL.md Stage 5.5 parses `flagged_gaps` and routes via AskUserQuestion. |
| Phase 7 fixtures README → Phase 4 wrapper signatures | OK | The README's calibration loop invokes `codex-validate-research.sh`, `codex-validate-plan.sh`, `codex-review-phase.sh` with their actual signatures (verified live in Phase 8 calibration where `codex-review-phase.sh` was invoked successfully against the f6 fixture). |
| Phase 7 `check-codex.sh` -e flag → ZAP-02 advisory contract | OK | `env -i PATH=/usr/bin:/bin bash check-codex.sh </dev/null` exits 0; SessionStart still never blocks Claude Code. |
| Phase 7 `check-wave-disjointness.sh` regex → f2 fixture overlap detection | OK | f2 fixture now exits 1 with OVERLAP diagnostic; production single-phase wave (f3) exits 0. |
| Phase 8 `codex-self-fix.sh` → `codex-review.sh` JSONL extractor | OK | Wrapper uses the same `codex-review.sh` path that the v1.0 audit + post-v1.0 CB-02 fix verified against codex-cli 0.133.0. Live round-trip in Phase 8 LOG confirms the call path. |
| Phase 8 `codex-self-fix.sh` → fixer-role prompt in `codex-prompts.md` | OK | The wrapper composes the prompt inline; `codex-prompts.md` documents the prompt shape for human-readable reference. CALIB-01 (SHA-256 ID derivation) preserved end-to-end (live codex preserved `ISS-23ba7d51473d` verbatim). |
| Phase 8 SKILL.md Stage 6.1 + 7c.1 → Phase 7 D-12 regex broadening | OK | The post-fix re-review path uses `codex-review-phase.sh`, which Phase 8 broadened to match fixture phase ids (mirrors Phase 7 ZAP-59's fix to `check-wave-disjointness.sh`). |
| Phase 8 f6 fixture → live codex round-trip | OK | 4-iteration round-trip documented in `live-codex-calibration-LOG.md`; final iteration produces a patch that (a) `git apply --check` accepts, (b) preserves CALIB-01 ID, (c) resolves the seeded HIGH finding upon post-fix re-review. |

## Anti-patterns scan

- TODO/FIXME in v1.1 code: none (`grep -RIn 'TODO\|FIXME' plugins/zapili/scripts/codex-self-fix.sh plugins/zapili/tests/fixtures/f6-fix-loop-exhausted` → no matches).
- Forbidden vocabulary (`key`, `main`, `top`, `important`) in shipped artifacts: NONE (codex incidentally fixed an instance during the live round-trip — `put(key, value)` → `put(id, value)` — which is a side-channel proof that the forbidden-vocabulary clause in the fixer prompt is being honored by the model).
- Empty stubs in new fixtures: none — every f6 file has substantive content.
- `.gitkeep` placeholders in new directories: none.

## Live codex calibration

- **Environment:** codex-cli 0.133.0, JSONL event shape `{"type":"item.completed","item":{"type":"agent_message","text":"..."}}`.
- **Round-trip iterations:** 4 (each surfaced and closed a real calibration gap; final iteration is the successful path persisted under `f6/.zapili/`).
- **Calibration-driven fixes made in-phase:**
  1. Fixer prompt tightened to require unified-diff context format (codex was emitting zero-context hunks).
  2. Patch-extraction AWK strip of whitespace-only lines (codex's `</patch>` indentation was being parsed as a phantom hunk line).
  3. `codex-review-phase.sh` phase-id regex broadened (mirrors Phase 7 ZAP-59; the post-fix re-review path needed it).
- **Result:** all 5 ZAP-60 acceptance criteria PASS against live codex.

## Post-audit ultra-principal review rounds (2026-05-29)

After the initial 2026-05-28 audit signed off at `passed`, the milestone went through a full ultra-principal code review pass (commits referenced below). The review surfaced issues the in-thread audit had not caught — confirming the value of independent post-audit review even on a `passed` milestone. All findings closed before this revision.

### Round 1 — 3 BLOCKERs + 3 WARNINGs (commit `c870186`, 2026-05-29)

| ID | Severity | Issue | Fix |
|----|----------|-------|-----|
| B-1 | BLOCKER | Orchestrator Stage 6.1 + 7c.1 invoked `codex-self-fix.sh` TWICE (dry-run then real) → applied a DIFFERENT patch than the one `git apply --check` validated. ZAP-60 core safety property "validate then apply same patch" broken. | `codex-self-fix.sh --dry-run` now emits patch FILE PATH on stdout; orchestrator captures path and runs `git apply $patch_file` directly. One codex call per cap-hit. |
| B-2 | BLOCKER | `f6/PLAN.md` mentioned `PHASE-XX` three times → `check-wave-disjointness.sh` treated as three distinct phases → false-positive OVERLAP → Stage 7.0 aborted; f6 fixture could not run end-to-end. | (a) Rewrote f6/PLAN.md Notes to avoid literal repetition; (b) added `sort -u` dedup to `check-wave-disjointness.sh` per-wave pids array as structural guard. |
| B-3 | BLOCKER | `state.schema.json` had `additionalProperties: false` and no `fix_loop_cap` property → ZAP-60's "configurable via `.zapili/state.json` `.fix_loop_cap`" failed schema validation. `iteration_counters` carried `maximum: 3` while SKILL.md defaulted cap to 4. | Added `fix_loop_cap` (integer 1–10) to schema; raised `iteration_counters` maximum from 3 to 10 for parity. |
| W-1 | WARNING | `codex-prompts.md` fixer scaffold missing 3 calibration-critical constraints (full path, 3-line context with worked example, positive framing) that were in the script but absent from "source of truth" docs. | Extended scaffold with all three REQUIREMENTS + worked example + positive-framing advisory. |
| W-2 | WARNING | Halt diagnostic hardcoded `attempt-1.patch` (always wrong after first attempt or post-B-1 fix). | Use `$patch_file` variable captured from dry-run. |
| W-3 | WARNING | Unresolved-IDs lookup wildcard `*.json` glob mixed already-resolved IDs from prior attempts with still-open ones; `tail -n+1` was no-op. | Use `$post_fix_findings` (latest only). |

### Round 2 — Exhaustiveness contract (commit `78a5f30`, 2026-05-29)

User-requested adaptation of an external "FULL review, not targeted" prompt template under our terminology (HIGH/MEDIUM/LOW, XML envelope, validation-findings.schema.json contract). The change defeats codex's default targeted-review behavior at the prompt level.

| Component | Change |
|-----------|--------|
| `validation-findings.schema.json` | +4 optional fields: finding-level `why_real_risk`, `repro`, `tests_to_add`; top-level `not_fully_audited[]`. Back-compat (old fixtures still validate). |
| `references/codex-prompts.md` | New "Exhaustiveness contract" section; severity mapping table (P0–P3 → HIGH/MEDIUM/LOW); finding evidence requirements; `not_fully_audited[]` explanation; updated Reclassification rules: "prior_findings are HYPOTHESES, not scope". |
| 3 validator scripts | Each gains `<exhaustiveness>` XML block with role-specific examples (e.g. "POST /api/auth with empty body returns 500" for phase_reviewer). |
| Updated valid example | Demonstrates every new field populated + `not_fully_audited` non-empty entry. |
| Live verification | Re-ran f1 fixture against live codex 0.133.0 with new prompt: HIGH+MEDIUM both populated `why_real_risk` + `repro` + `tests_to_add`; expected `ISS-cc94a3aa8710` preserved; `not_fully_audited: []` (legitimate full coverage on small fixture). |

## Deferred / open items (acknowledged tech debt — NOT release blockers)

| Item | Status | Routed to |
|------|--------|-----------|
| `codex-review-phase.sh` regex parity note in `references/contracts.md` | DEFERRED | Worth a contracts.md advisory: "any future PHASE-id parser MUST use `[A-Za-z0-9]+`". 2-line doc addition; can land in v1.2 polish. |
| `codex-self-fix.sh` prompt heredoc absolute `$PROMPTS_REF` path (review NIT-1) | DEFERRED | Replace with `${CLAUDE_PLUGIN_ROOT}/...` or drop reference (rule context is already in the prior_findings block). 1-line fix; v1.2 polish. |
| `f2/PLAN.md:8` stale `LINES 8-25` comment (review NIT-2) | DEFERRED | Trivial cosmetic; v1.2 polish. |
| `codex-prompts.md` halt-paths table "0 (from script)" annotation (review NIT-4) | DEFERRED | Split into Script-exit / Workflow-outcome columns; v1.2 polish. |
| End-to-end Stage 6.1 / 7c.1 integration smoke test | DEFERRED | The live calibration in Phase 8 covers the `codex-self-fix.sh` wrapper round-trip; the orchestrator's Stage 6.1 / 7c.1 wiring is verified by inspection (SKILL.md prose). A full orchestrator dry-run requires a real `/zapili:zapili` invocation, which is release-maintainer scope (same as v1.0 audit's deferred items). |

## v1.0 audit gaps — status check

The v1.0 audit listed 4 release-maintainer manual steps. These are NOT v1.1's responsibility (v1.1 is a hardening milestone, not a re-release), but for completeness:

| v1.0 gap | v1.1 disposition |
|----------|-----------------|
| Live install rehearsal post-version-bump | Still maintainer-owned. v1.1 should bump `plugin.json` version to `1.1.0` at release time — same rehearsal applies. |
| Live smoke-test round-trip | Unchanged (still maintainer-owned). |
| Live chaos rehearsal | Unchanged (still maintainer-owned). |
| Live codex review calibration against f1..f5 | Phase 8 live-validated the new `codex-self-fix.sh` against f6. Validators against f1..f5 remain maintainer-owned; the calibration LOG in Phase 8 establishes the technique (prompt + ID derivation + JSONL parse) is sound. |

## Gaps

### CRITICAL — none

### NON-CRITICAL (release-time maintainer activities, inherited from v1.0)

1. **Live install rehearsal** after `plugin.json` version bump (v1.0.0 → v1.1.0).
2. **Live smoke-test round-trip** on `smoke-small-task`.
3. **Live chaos rehearsal** (11 boundaries).
4. **Live codex calibration on f1..f5** (Phase 8 calibrated against f6; f1..f5 carry over from v1.0).

These are gated on a live Claude Code session + codex auth and remain the release maintainer's responsibility.

## Status: passed

All 6 v1.1 requirements implemented + verified (including a live codex round-trip for ZAP-60). All cross-phase integration boundaries OK. Live codex calibration produced a clean acceptance result on the f6 fixture. Cumulative milestone state: 8/8 phases complete, 26/26 plans complete, 49/49 requirements complete.

## Recommendation

**Ready to archive.** As of 2026-05-29:
- `plugins/zapili/.claude-plugin/plugin.json` version bumped to **1.1.0**.
- `CHANGELOG.md` v1.1.0 entry landed.
- Post-audit ultra-principal review rounds (Round 1 = 3 blockers + 3 warnings → all fixed; Round 2 = exhaustiveness contract → calibrated live) closed.

Remaining maintainer-owned activities before push (inherited from v1.0):
1. Live install rehearsal in fresh Claude Code session (`/plugin marketplace add nepavel/oplya` + `/plugin install zapili@oplya`).
2. Live smoke-test round-trip on `smoke-small-task`.
3. Live chaos rehearsal (11 boundaries).
4. Live codex calibration on f1..f5 (Phase 8 already calibrated against f6; f1 separately re-verified during the exhaustiveness contract live test; f2..f5 calibration evidence committed alongside this audit revision).

After those, run `/gsd-complete-milestone v1.1` to archive.
