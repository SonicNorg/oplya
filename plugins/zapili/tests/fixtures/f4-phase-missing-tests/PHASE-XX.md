# PHASE-XX — add JWT issuance

<files>{"writes": ["src/auth/jwt.ts", "src/auth/jwt.test.ts"], "reads": []}</files>

## Tasks

1. Implement `issueToken()` in `src/auth/jwt.ts`.
2. Add unit tests covering the happy path and signature-verification failure in `src/auth/jwt.test.ts`.
