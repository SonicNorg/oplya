# Task — JWT issuance

Implement a stateless JWT issuance helper for the auth service, with unit tests
covering token issuance, signature verification, expiry handling, and the
malformed-input rejection path.
