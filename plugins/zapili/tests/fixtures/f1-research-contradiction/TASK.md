# Add JWT auth to /login endpoint

**Stack:** Node 20 + Express + Postgres.
**Constraint:** Must use the existing `jsonwebtoken@8.x` library — do NOT introduce a new auth dependency.
**Reference:** see `src/auth/` for current shape.
