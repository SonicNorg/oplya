# Task — Kotlin coroutine login refactor

Port the existing `login()` flow to idiomatic Kotlin coroutines (`flow {}` builder
+ null-safety) so that the auth service matches the rest of the codebase's
coroutine-first style.
